<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <comment/>
        <!-- key -->
        <entry key="selectEmpByLastName">
            <!-- value -->
            SELECT
            *
            FROM
            employee
            WHERE
            emp_name LIKE CONCAT (?, '%')
        </entry>

        <entry key="selectEmpByEmpId">
            <!-- value -->
            SELECT
            *
            FROM
            employee
            WHERE
            emp_id = ?
        </entry>
</properties>